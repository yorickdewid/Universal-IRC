using Terminal.Gui;

namespace UniversalIRC.RelayChat.TerminalUI
{
    internal class CanvasWindow : Window
    {
        public CanvasWindow(string title)
            : base(title)
        {
            //AddLayoutView();
            //AddLayoutAction();
        }

        public CanvasWindow(Rect frame, string title)
            : base(frame, title)
        {
            //AddLayoutView();
            //AddLayoutAction();
        }
    }
}
